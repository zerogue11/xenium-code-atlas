# 04 测试记录（三期使用，一期占位）

> 三期实操测试遵循 `F:\xenium数据\ov工作流测试\AGENT.md` 纪律执行，届时每测一例在此建目录：
> `测试_<对象>_<范围>_<日期>/`，内含 scripts/logs/results + 测试记录.md，失败写 run_failed.json。

## 预定测试序列（占位，未执行）

1. readiness：环境探测（包版本/GPU/依赖）。
2. smoke：V1 肝癌公开数据最小回归（对照已冻结的官方线结果）。
3. 递进：5k→200k→500k→all（ZXM）。
4. 高级模块逐个 smoke（A1–A9）。

一期不执行任何测试。
